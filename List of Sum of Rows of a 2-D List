def  sum_of_2d(li):
    sum=0
    sumlist=[]
    for i in li:
        sum=0
        for j in range(len(i)):
                       sum=sum+i[j]
        sumlist.append(sum)
                       

    return sumlist

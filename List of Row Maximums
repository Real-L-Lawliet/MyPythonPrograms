def  sum_of_2d(li):
    max=0
    sumlist=[]
    for i in li:
        max=0
        for j in range(len(i)):
                       if max<i[j]:
                           max=i[j]
        sumlist.append(max)                   
        

    return sumlist

def count_consonants(x):
    x=x.lower()
    count=0
    vowels=['a','e','i','o','u',' ']
    for i in x:
        if i not in vowels:
            if ord(i)<=125 or ord(i)>=90:
                count=count+1

    return count
